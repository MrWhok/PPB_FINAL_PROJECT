name: ppb_final_project
description: "DebateCoach — AI-powered debate training app"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_messaging: ^15.0.0
  firebase_storage: ^12.0.0
  firebase_crashlytics: ^4.0.0
  google_generative_ai: ^0.4.0
  flutter_tts: ^4.0.0
  http: ^1.2.0
  intl: ^0.19.0
  google_sign_in: ^6.0.0
  record: ^6.0.0
  path_provider: ^2.1.4
  flutter_local_notifications: ^22.0.0
  timezone: ^0.11.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
